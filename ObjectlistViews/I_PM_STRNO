define view /ETN/I_PM_STRNO  as select from iflos
{
key tplnr as Funcloc,
    strno as FunclocLabel

}
where iflos.actvs = 'X'
  and iflos.prkey = 'X'  // Insert N16951 CD: 7300011426 - strno not unique -> primäres Kennzeichensystem (Meier Andreas)
